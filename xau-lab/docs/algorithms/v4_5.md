# V4.5 — profit-velocity research line

Status: **research in progress; not locked**

V4.5 starts from locked V4.4 and now treats provisional Candidate D as the research leader while the project shifts from capture-only optimization toward **profit velocity**: maximizing expected XAUUSD profit per unit time without relaxing the fixed risk discipline.

## Canonical baseline

All V4.5 candidates must use the parity-gated canonical replay contract:

- `research/canonical_replay.py`
- `results/regression/canonical_v4_4_reference.json`
- `docs/replay/CANONICAL_REPLAY.md`

A candidate is invalid if locked V4.4 fails to reproduce its canonical regression oracle on the same feature build.

## Verified comparator — Candidate B

Candidate B preserves V4.4 PRIMARY / SECONDARY / MICRO and adds an independent BURST engine after all three decline the sample.

Canonical Candidate B results:

- 500 ms: +₹1,299.37, 17.3652% capture, 163 trades, median segment PF 1.361;
- 1 s: +₹365.25, 4.8814% capture, 156 trades, median segment PF 1.382.

The final 20% holdout remains unopened. Candidate B was the verified leader through Experiment 18 and remains the parity comparator for the next candidate.

### BURST admission

BUY requires:

- PRIMARY / SECONDARY / MICRO all decline;
- cooldown >= 60 s;
- 10 s momentum >= +$0.60;
- 30 s momentum >= +$1.50;
- 60 s momentum >= +$2.00;
- 120 s momentum positive;
- ER60 >= 0.12;
- 10 s / 30 s quote-rate ratio >= 1.20;
- directional 10 s raw-tick imbalance >= +0.20;
- 60 s range >= $4;
- spread <= 18% of 60 s range;
- absolute spread <= $0.28.

SELL is symmetric.

### Candidate B lifecycle

- emergency stop: $4;
- take profit: $12;
- max hold: 120 s;
- 30 s momentum zero-cross failure exit;
- stagnation after 60 s if peak favorable move < $0.80;
- trail after +$5.50 with $2 give-back.

## V4.5-C Phase C1 — completed negative result

Phase C1 kept Candidate B admission unchanged and searched BURST lifecycle behavior for better profit velocity and lower sampling fragility.

It tested 48 combinations of:

- max hold: 120 / 180 s;
- trail trigger: $5.50 / $6.50 / $7.50;
- trail give-back: $2.00 / $2.50;
- optional raw-flow continuation extension;
- optional flow-decay early exit.

All 48 seed variants reproduced Candidate B exactly at both 500 ms and 1 s.

The top eight chronological evaluation candidates and top three full first-80% summaries also reproduced Candidate B exactly.

Therefore Phase C1 produced **no distinct Candidate C** and no promotion.

The result indicates that these lifecycle controls are shadowed/inactive on the observed BURST trade paths. Earlier BURST exit conditions and/or entry timing dominate before the tested knobs can change realized outcomes.

Evidence:

- `docs/experiments/2026-09-24/17-v4-5-c-profit-velocity.md`
- `results/simulations/2026-09-24-v4_5-c-profit-velocity/`

## Profit-velocity diagnostics now available

Candidate B canonical instrumentation now records:

### 500 ms

- exposure: 15.823 h;
- P&L / exposure hour: ₹58.36;
- mean hold: 349.47 s;
- P&L / trade: ₹5.67;
- BURST P&L: +₹58.40 across 17 trades;
- BURST P&L / exposure hour: ₹232.98.

### 1 s

- exposure: 17.248 h;
- P&L / exposure hour: ₹18.06;
- mean hold: 398.03 s;
- P&L / trade: ₹2.00;
- BURST P&L: +₹10.44 across 12 trades;
- BURST P&L / exposure hour: ₹67.49.

The weak 50-60% region remains:

- 500 ms: -₹13.58;
- 1 s: -₹45.95.

## Experiment 18 autopsy and provisional Candidate D

The parity-gated BURST autopsy found every Candidate B BURST trade exited on the first 30 s momentum zero-cross: 17/17 at 500 ms and 12/12 at 1 s. This exit precedes the Phase C1 lifecycle controls. The autopsy also found 28 BURST threshold episodes at 500 ms versus 25 at 1 s; six 500 ms episodes had no matched 1 s episode. Seven actual 500 ms BURST trades lacked a matched 1 s BURST trade. The whole-system 1 s loss is larger than the BURST component, with PRIMARY and SECONDARY contributing most of the summed-segment degradation.

Candidate D changes exactly one BURST lifecycle rule: a 30 s momentum zero-cross must persist for **one second** before it causes a failure exit; the timer resets if momentum recovers or is unavailable. Admission, engine priority, V4.4 engines, bid/ask accounting, the 3% planned risk, $4 stop, shared slot and other BURST exits remain Candidate B rules. One second was selected on the 0–40% seed because it improved P&L on both grids; 2/3 s full-run variants and a 5 s seed rejection are preserved in Experiment 19.

Canonical first-80% Candidate D results, after V4.4 golden and Candidate B instrumentation parity:

| Metric | 500 ms | 1 s |
| --- | ---: | ---: |
| Compounded P&L | +₹1,430.31 | +₹385.25 |
| Capture | 19.1152% | 5.1487% |
| Trades / wins | 163 / 81 | 156 / 71 |
| Median segment PF | 1.376 | 1.378 |
| Worst segment drawdown | ₹218.94 | ₹172.14 |
| P&L per exposure hour | ₹61.38 | ₹18.79 |
| BURST P&L | +₹100.85 | +₹22.04 |

Candidate D is the **provisional research leader, not locked**. Relative to B, 1 s PF is slightly lower and 500 ms worst-segment drawdown slightly higher. The 50–60% segment remains negative (-₹13.58 / -₹45.95), 1 s capture remains far below the 500 ms result, and an additional $0.20 adverse slippage per side reduces the fixed-size 1 s sensitivity to about ₹51.42. Full execution-aware resimulation and broader sampling-regime diagnostics remain necessary before any V4.5 lock.

Evidence:

- `docs/experiments/2026-09-24/18-v4-5-burst-path-autopsy.md`
- `results/simulations/2026-09-24-v4_5-burst-path-autopsy/`
- `docs/experiments/2026-09-24/19-v4-5-confirmed-failure.md`
- `results/simulations/2026-09-24-v4_5-confirmed-failure/`

Experiment 19 pointed to the non-BURST PRIMARY/SECONDARY sampling loss and the 50–60% regime. Experiment 20 below records the subsequent diagnostic. Do not repeat the blind C1 lifecycle grid.

## Experiment 20 — exposure and sampling diagnostic

PRIMARY and SECONDARY use most of Candidate D's exposure. At 1 s, nine unmatched PRIMARY trades lost ₹174.04 across 2.46 exposure hours, and 22 unmatched SECONDARY trades lost ₹141.28 across 1.86 hours. Those matching categories are heuristic and do not prove a causal admission defect.

Experiment 20 tested conditional release at 120/300 s, two-second SECONDARY persistence, and a bounded 10 s momentum / quote-acceleration veto ablation on the 0–40% seed. None improved the joint 500 ms and 1 s profit-velocity frontier. For example, m10-only veto raised 1 s seed P&L from ₹169.92 to ₹317.74 and cut exposure, but reduced 500 ms seed P&L from ₹696.84 to ₹418.10. No variant progressed to later evaluation or cost stress. Candidate D remains provisional; no paper version is created.

The 50% profit-velocity, 20% exposure-reduction and 20% compounded-P&L gains are a **preferred strong-lock region**, not automatic promotion thresholds. A future candidate may be lock-eligible pending review after a narrow miss if its aggregate Pareto improvement is material. Minor PF/drawdown deterioration may be tolerated only when clearly compensated, documented and reviewed. A seed-only gain never locks V4.5.

Experiment 21 then tested an independent, risk-budgeted, at-most-three-ticket trend/impulse/reversal research architecture. Its initial full first-80% prototype lost at both grids, and two seed revisions failed joint-grid selection. It did not alter the single-slot Candidate D architecture or earn a paper version.

Experiment 22 froze a separate recent MT5 24-hour quote snapshot. Historical V4.4 golden parity passed on both grids before the fresh-data replay. Candidate D's realized-only recent seed result was -₹92.48 / -₹86.54 at 500 ms / 1 s, mostly from PRIMARY. The separate multi-ticket mode 1's trend tickets also lost on both seed grids. One seed-derived impulse-only ablation (mode 3) yielded +₹3.73 / +₹7.92 on seed but **-₹1.83 / -₹1.81 on the later evaluation**, worsening to -₹6.47 / -₹3.37 with an extra $0.20 adverse fill per side. The continuous 24-hour mode 3 result was only +₹1.89 / +₹6.08 before cost stress. This was far short of ₹100 per reported quote day and was rejected for promotion. The MT5 tick clock was about three hours ahead of host UTC; date labels in this snapshot are not verified civil UTC.

The fresh single-slot figures are realized-only and may exclude an open position at a segment boundary, while the independent ticket simulator forces a close. This limits cross-architecture ranking, but the mode 3 rejection is supported by like-for-like later evaluation and cost-stress comparisons within its own simulator. Candidate D remains the **provisional historical research leader, not locked**. The recent negative regime makes a V4.5 lock less supportable, not more. V4.4 remains locked and the final holdout sealed.

## Experiment 23 — raw-event regime-normalization foundation

Experiment 23 changes no entry, exit, sizing or risk rule. It introduces a diagnostic market-state layer calculated from the underlying raw tick stream **before** 500 ms or 1 s execution-grid sampling. The frozen representation uses lagged relative range/volatility, spread, path efficiency and quote activity rather than P&L-selected absolute regime thresholds.

V4.4 golden parity, Candidate B trace parity and Candidate D parity passed on both grids. Candidate D reproduced +₹1,430.31 / 163 trades / 81 wins at 500 ms and +₹385.25 / 156 trades / 71 wins at 1 s. Across 119 same-engine/same-side entries matched within 120 seconds, the raw-event categories agreed between grids on volatility 92.44%, spread 89.92%, efficiency 92.44%, activity 95.80% and the complete four-part regime key 76.47%.

The four-hour engine audit is diagnostic rather than a permission table. BURST was positive in 9 versus 2 negative 500 ms windows and 6 versus 4 at 1 s. MICRO was 10/6 and 10/5. PRIMARY was 10/10 and 9/9 with a negative median four-hour P&L on both grids despite positive aggregate P&L. SECONDARY was 6/8 and 7/9 and its 1 s aggregate P&L was -₹51.80.

No engine is disabled from the same dataset that revealed these weaknesses. Replay `xau-raw-event-regime-v1` unchanged on non-overlapping recent/future snapshots before selecting a deterministic regime router.

Evidence: `docs/experiments/2026-09-25/23-v4-5-regime-normalization.md` and `results/simulations/2026-09-25-v4_5-regime-normalization/`.

## Experiment 24 — bounded regime-router probe

The first router experiment deliberately kept the search small and interpretable. Using only the 0-40% seed, it tested exactly 48 one-category vetoes: four engines × four raw-event regime dimensions × three categories. A veto was seed-eligible only if it improved P&L on both grids, did not lower win rate on either grid, retained at least 95% of Candidate D's trades on both grids, and kept max drawdown within 2% of the seed baseline.

Only two variants qualified. The stronger joint-grid seed result vetoed MICRO when raw relative 60-second range was below 0.75. It was selected before later evaluation: 500 ms seed P&L rose from ₹696.84 to ₹709.36 with 97 versus 98 trades, and 1 s rose from ₹169.92 to ₹179.60 with 97 versus 98 trades.

The frozen veto was then evaluated once on the later 40-80% pool. Full first-80% results were:

| Metric | Candidate D 500 ms | Router probe 500 ms | Candidate D 1 s | Router probe 1 s |
| --- | ---: | ---: | ---: | ---: |
| Compounded P&L | ₹1,430.31 | **₹1,407.86** | ₹385.25 | **₹398.05** |
| Trades | 163 | 161 | 156 | 155 |
| Wins | 81 | 80 | 71 | 71 |
| Win rate | 49.693% | 49.689% | 45.513% | 45.806% |
| Worst segment DD | ₹218.94 | ₹221.23 | ₹172.14 | ₹164.53 |

The probe therefore fails the joint-grid objective. It slightly improves 1 s robustness but gives back ₹22.45 of 500 ms compounded P&L and does not materially improve win rate. It is **rejected** and Candidate D remains provisional.

Do not now promote or evaluate the second seed-qualified veto simply because the selected probe failed later evaluation; doing that would turn the known 40-80% pool into an iterative selection set. The next router work should first use independent non-overlapping market windows.

Evidence: `docs/experiments/2026-09-25/24-v4-5-regime-router-probe.md` and `results/simulations/2026-09-25-v4_5-regime-router-probe/`.

## Experiment 25 — cross-window engine robustness baseline

Experiment 25 made no strategy change and no selection. It compared preserved Candidate D engine attribution from the canonical seven-day history with Experiment 22's separate recent 24-hour seed/evaluation ledgers.

At 500 ms, PRIMARY moved from +₹586.30 historically to -₹184.37 across the recent split ledgers; SECONDARY from +₹89.67 to -₹27.96; MICRO from +₹197.69 to -₹20.71; and BURST from +₹100.85 to -₹9.36. At 1 s, PRIMARY moved from +₹156.00 to -₹118.65; MICRO from +₹198.36 to -₹12.28; BURST from +₹22.04 to -₹8.86. SECONDARY was already negative historically at 1 s (-₹51.80) and was approximately flat in the recent split (+₹0.93).

The recent MICRO/BURST counts are tiny, so this does not justify disabling them. Engine identity alone is not a robust routing variable. Future admission must combine engine identity with the frozen raw-event regime state and independent-window evidence, preserve an explicit HOLD/no-trade outcome, and track opportunity retention.

The Experiment 22 single-slot ledgers are realized-only and boundary-sensitive, so these recent figures are descriptive engine attribution rather than terminal-equity full-window returns.

Evidence: `docs/experiments/2026-09-25/25-v4-5-cross-window-engine-robustness.md` and `results/simulations/2026-09-25-v4_5-cross-window-engine-robustness/`.

## Experiment 26 — frozen regime portability on recent raw data

Experiment 26 applied the exact `xau-raw-event-regime-v1` representation from Experiment 23 to Experiment 22's non-overlapping recent 24-hour raw snapshot. No regime threshold, strategy parameter, engine rule or candidate was tuned.

The uploaded/archived snapshot verified against both committed hashes: gzip SHA-256 `2d3c963f678aa0341224efe5f6859651018e11a43c8c5797bb5685f8dce19aeb` and decompressed raw SHA-256 `9243ac73c3d180417ff634fe9371a28939713a531c9ccfa63f919c94eb9a28b1`.

Across 27 same-split, same-engine, same-side recent Candidate D entries matched between the 500 ms and 1 s grids within 120 seconds, category agreement remained high: 92.59% volatility, 92.59% spread, 96.30% efficiency, 100% activity, and 85.19% for the complete four-part regime key. The representation is therefore portable as a description of market state.

It is **not** yet portable as a profit permission map. When each recent trade was classified only by whether its exact engine+grid+four-part key had positive historical P&L, historically positive keys produced 23 trades / 4 wins / -₹90.74 at 500 ms and 21 trades / 4 wins / -₹123.88 at 1 s. Historical non-positive keys produced 14 trades / 1 win / -₹145.21 at 500 ms and 10 trades / 4 wins / +₹4.74 at 1 s. Two 1 s trades and one 500 ms trade were unseen keys.

PRIMARY remains the clearest failure source. On the recent snapshot, its 500 ms exits were 13 emergency stops (-₹172.84), 7 momentum-zero-cross exits (-₹51.34), and 4 max-hold exits (+₹39.82). At 1 s they were 9 emergency stops (-₹129.78), 6 zero-cross exits (-₹39.24), and 5 max-hold exits (+₹50.37). The surviving max-hold opportunity class still works; the recent problem is that too many admissions terminate as stop/zero-cross failures.

Therefore the next representation must preserve the current causal normalized state but add richer continuous context, especially direction-normalized short/long momentum and market-heat/exhaustion diagnostics, **without selecting thresholds from this already observed recent window**. Freeze formulas first, then validate on a new non-overlapping snapshot before another router search.

Evidence: `docs/experiments/2026-09-25/26-v4-5-regime-portability.md` and `results/simulations/2026-09-25-v4_5-regime-portability/`.

## Experiment 27 — `xau-state-v2` feature freeze

Experiment 27 freezes a richer raw-event state representation before the next unseen market window. It makes no entry, exit, sizing, priority or router change and selects no feature threshold from trade P&L.

`xau-state-v2` contains 20 continuous features: normalized 60 s range, spread and 10 s activity; ER60 and spread/range load; direction-normalized 10/30/60/300 s momentum; direction-normalized path efficiency; 10 s and 60 s raw flow alignment; short-vs-long momentum acceleration/balance; 60 s and 300 s pullback depth; market heat; execution heat; and session age. All rolling reference distributions use completed, lagged buckets only.

The formulas were defined structurally before outcome profiling. Across 319 known historical Candidate D trades and 71 known recent trades, all short-horizon state fields had 100% finite coverage. The 300 s-derived fields had 96.9% historical and 95.8% recent coverage because the causal baselines require warm-up after gaps.

Cross-grid stability remained strong. On 119 historical matched entries, the median feature Spearman correlation between 500 ms and 1 s was 0.916; 17/20 features were at least 0.80 and 11/20 at least 0.90. On the recent 27 matched entries, the median was 0.940; 18/20 features were at least 0.80 and 14/20 at least 0.90. The weakest features were pullback60 and spread-relative state; they are retained as continuous context, not standalone gates.

Descriptive PRIMARY profiles continue to support the admission/exhaustion hypothesis without creating a threshold. Recent max-hold winners occurred with lower median market heat than stop/zero-cross failures, while acceleration and pullback context also differed. These observations are **not** selection evidence because both windows are already known.

### Pre-registered Router v2 direction

The next router must evaluate engines as opportunity generators rather than globally good/bad modules. If a higher-priority engine is rejected by its state score, the simulator should continue to lower-priority eligible engines instead of automatically deleting the trade opportunity. HOLD remains valid only when no engine has demonstrated a sufficiently strong state.

For the first unseen-window Router v2 evaluation, the target is: higher net P&L and win rate than Candidate D on both grids, at least 95% system-level trade-count retention (or replacement opportunities that keep total count within 5%), no material drawdown deterioration, and preserved cost robustness. No router is promoted until a genuinely new non-overlapping snapshot is evaluated.

Evidence: `docs/experiments/2026-09-25/27-v4-5-state-v2-freeze.md` and `results/simulations/2026-09-25-v4_5-state-v2-freeze/`.

## Experiment 28 — stronghold-aware Router v2 implementation

Experiment 28 implements the first actual Router v2 mechanics while keeping Candidate D's engine signals, lifecycle, sizing, spread handling and risk exits unchanged.

When flat, the router no longer stops at the first fixed-priority signal. It gathers every currently eligible PRIMARY, SECONDARY, MICRO and BURST candidate, attaches the frozen `xau-state-v2` state, computes an engine-specific structural stronghold score, and selects the highest score. If the best candidate is below neutral, the router falls through to the next candidate; if none clears neutral, it emits HOLD.

The score definition is frozen before unseen evaluation. Each engine uses seven equal-weight components with no P&L weight search. Direction-normalized momentum/flow, pullback-to-favorable-edge, range/activity expansion and execution/market heat are combined according to the engine's semantic role. Continuous ratios are centered at their causal rolling baseline (ratio 1 = neutral), directional momentum uses a fixed tanh squash scale of 3, and the qualifying score floor is exactly 0.0. Ties preserve Candidate D's legacy engine priority.

The implementation includes a legacy-priority parity mode so Candidate D's lifecycle can be checked segment-by-segment before Router v2 diagnostics. Known data can be used for that implementation check only; it cannot be used to change weights, the squash scale, score floor or component list.

At the Experiment-28 freeze point, no Router v2 performance result had been accepted. The pre-registered promotion target was higher net P&L and win rate than Candidate D on both grids, at least 95% system-level trade retention, no material drawdown deterioration and later execution-cost stress. Experiment 30 subsequently supplied the full **known-data** raw-tick diagnostic and did not promote the frozen score; genuinely unseen non-overlapping data is still required before any future router can be promoted.

Evidence: `docs/experiments/2026-09-25/28-v4-5-router-v2-implementation.md` and `results/simulations/2026-09-25-v4_5-router-v2-implementation/`.

## Experiment 29 — Router v2 known-ledger preflight

Experiment 29 is deliberately weaker than a raw-tick replay. It takes the realized Candidate D entry ledgers enriched with `xau-state-v2`, applies the frozen Router-v2 score floor to each realized entry, and treats rejected entries as HOLD. It **cannot** create lower-priority same-time replacement trades, so it is a gate proxy rather than the full Router-v2 arbitration architecture.

The Candidate D ledger reproduces the canonical first-80% result exactly: +₹1,430.31 / 163 trades / 81 wins at 500 ms and +₹385.25 / 156 trades / 71 wins at 1 s. The proxy retained 159 / 163 trades at 500 ms and 151 / 156 at 1 s, but compounded P&L fell to ₹1,181.58 and ₹219.73 respectively. The rejected historical trades were all PRIMARY and had net positive realized P&L: +₹163.99 at 500 ms and +₹124.99 at 1 s.

On Experiment 22's recent ledger, the proxy rejected one losing PRIMARY trade per grid. Seed results were unchanged; later realized evaluation improved from -₹149.92 to -₹135.52 at 500 ms and from -₹52.31 to -₹37.90 at 1 s. Across both recent splits, trade retention remained 97.37% / 96.97%, but total realized attribution remained strongly negative.

A seeded 10,000-scenario empirical regime-mixture bootstrap sampled 40 trade opportunities per scenario from random mixtures of the historical and hostile recent ledgers and compounded each trade's inferred return. The proxy did not improve the central distribution: at 500 ms median P&L was -₹70.60 versus Candidate D -₹68.67, with 34.04% versus 35.26% profitable scenarios; at 1 s median P&L was -₹58.97 versus -₹58.96, with 30.91% versus 33.06% profitable scenarios. The proxy slightly reduced the 5% loss tail but also cut the positive tail.

This diagnostic therefore gives **no basis to promote Router v2 or tune its frozen score on known data**. Candidate D remains the current winner. The only valid next strategy decision is a full raw-tick fall-through replay on a new non-overlapping snapshot.

Evidence: `docs/experiments/2026-09-25/29-v4-5-router-v2-preflight.md` and `results/simulations/2026-09-25-v4_5-router-v2-preflight/`.

## Experiment 30 — full raw-tick Router v2 replay

Experiment 30 completed the end-to-end raw-tick evaluation of the frozen Experiment-28 arbitration design. Candidate D canonical parity passed exactly before Router-v2 comparison.

On the canonical first80, Router v2 preserved the full trade count but materially reduced quality: 500 ms fell from **+₹1,430.31 / 163 trades / 81 wins** to **+₹851.27 / 163 / 78**, while 1 s fell from **+₹385.25 / 156 / 71** to **+₹297.67 / 156 / 70**. The 500 ms worst segment drawdown improved from ₹218.94 to ₹163.44, but the P&L and win-rate loss is too large; 1 s drawdown worsened.

On the whole recent 24-hour raw snapshot, Router v2 modestly improved 500 ms from -₹223.27 to -₹210.63 with 38 versus 39 trades, but worsened 1 s from -₹135.91 to -₹144.22 while increasing trades from 34 to 36. Neither grid became profitable.

Corrected deterministic four-hour windows were sampled only inside continuous real sessions from the known canonical/recent datasets. Mean P&L was -₹6.26 Candidate D versus -₹8.89 Router v2 at 500 ms and -₹8.25 versus -₹15.23 at 1 s. Recent-window overlap means this is stress evidence, not independent-day validation.

Therefore **the frozen Router-v2 score is not promoted and Candidate D remains the V4.5 leader**. Do not tune the score against this known evidence. The next research task is a decision-level arbitration audit to identify whether damage comes from engine substitution, HOLD/re-entry, cooldown-history divergence or same-engine timing changes.

Evidence: `docs/experiments/2026-09-25/30-v4-5-router-v2-full-replay.md` and `results/simulations/2026-09-25-v4_5-router-v2-full-replay/`.

## Experiment 31 — Router v2 arbitration divergence audit

Experiment 31 makes **no strategy change**. Its instrumented Candidate-D and Router-v2 engines reproduce Experiment 30 exactly, then trace every arbitration divergence on the canonical first80 and known recent 24-hour data.

Only 48 decision divergences occur across both grids and windows. Direct same-time score substitutions are not large enough to explain the failure: their fixed-₹500 isolated Router-minus-D delta is only -₹2.33 historically at 500 ms and -₹8.80 at 1 s.

The dominant mechanism is path dependence. A changed engine or HOLD alters engine-specific cooldowns and shared-slot occupancy; that changes later entry timing and which opportunities exist. On the historical 500 ms 0-40% segment, the exact -₹353.97 segment gap decomposes into -₹148.85 from Candidate-D-only trades, -₹74.81 from Router-only losses, -₹51.06 from same-engine timing shifts and -₹79.25 from lower balance-based sizing on 90 otherwise identical trades. The immediate simultaneous substitution in that segment contributes ₹0 realized difference by itself.

The clearest HOLD examples rejected profitable PRIMARY entries with scores only slightly below zero: fixed-₹500 exact-entry counterfactuals of +₹61.39 at score -0.01635 and +₹43.16 at score -0.03317. But losing rejected entries occupy the same negative-score region. Lowering the floor to recover those winners would therefore be an outcome-fitted threshold, not supported generalization.

At 1 s the main 0-40% loss is also timing/occupancy driven: a +₹28.33 Candidate-D MICRO trade is replaced by a -₹22.46 Router SECONDARY trade 28 seconds later, a -₹50.79 swing.

The recent window confirms both sides of the mechanism. At 500 ms the Router gains ₹12.64 mainly by omitting two losing Candidate-D PRIMARY tickets, but at 1 s three Router-only follow-on trades lose -₹32.52 and erase the avoided-loss benefit.

Therefore Candidate D remains the V4.5 leader and the frozen Router-v2 score remains unpromoted. The next architecture should preserve Candidate D priority, define causal signal episodes so rejected opportunities cannot simply re-enter later as the same episode, and account for shared-slot opportunity cost before fallback. Do not sweep the known score floor.

Evidence: `docs/experiments/2026-09-25/31-v4-5-router-v2-arbitration-audit.md` and `results/simulations/2026-09-25-v4_5-router-v2-arbitration-audit/`.

## Experiment 32 — priority-preserving, rejection-latched Router v3

Router v3 was frozen before performance evaluation. It preserved Candidate D engine priority, used the unchanged Experiment-28 score only as a veto and latched a vetoed engine for its pre-existing Candidate-D cooldown horizon.

Known-data results did **not** support promotion. Canonical first80 was +₹1,162.46 / 159 trades / 79 wins at 500 ms and +₹214.06 / 155 / 70 at 1 s, versus Candidate D +₹1,430.31 / 163 / 81 and +₹385.25 / 156 / 71. Recent whole-window P&L improved modestly versus D but remained negative: -₹210.63 at 500 ms and -₹128.87 at 1 s.

The key structural observation is that canonical fallback entries were **zero**. All four 500 ms vetoes and all five 1 s vetoes were PRIMARY-only opportunities. The architecture therefore behaved as sparse PRIMARY gating rather than useful engine substitution.

Known four-hour stress also failed to show broad advantage: Router v3 beat Candidate D in 15% of 500 ms windows and 2.5% of 1 s windows.

Candidate D remains the V4.5 leader. Further router tuning is paused pending an independent-engine opportunity atlas.

Evidence: `docs/experiments/2026-09-25/32-v4-5-router-v3-priority-latched.md` and `results/simulations/2026-09-25-v4_5-router-v3-priority-latched/`.

## Experiment 33 — independent engine opportunity atlas

Experiment 33 removes shared-slot competition, cross-engine cooldown cascades and compounding by replaying each existing engine as its own fixed-₹500 shadow lane.

Historical first80 fixed-size P&L is positive for all four engines on both grids. The later recent window is not: at 500 ms PRIMARY -₹255.11, SECONDARY -₹45.49, MICRO -₹34.46 and BURST -₹17.44; at 1 s only SECONDARY remains positive (+₹18.82 on nine trades) while PRIMARY, MICRO and BURST lose.

Independent signals are rarely simultaneous: only about 3.5% of historical entry events and 2.2–2.4% of recent events involve multiple engines at the same timestamp. This explains why fall-through routing has little room to create a different opportunity set.

The current four-engine family therefore lacks sufficient portable complementarity by itself. Another hand-tuned priority/score rule is not justified.

Evidence: `docs/experiments/2026-09-25/33-v4-5-independent-opportunity-atlas.md` and `results/simulations/2026-09-25-v4_5-independent-opportunity-atlas/`.

## Experiment 34 — SNAPBACK complementary-engine foundation

Experiment 34 tested a structurally different failed-continuation/countertrend lane frozen before P&L evaluation. It reused existing Candidate-D/MICRO quality thresholds with the natural low-efficiency complement `ER60 < 0.12`, reversed 10 s momentum/flow against the background move, and reused the short BURST lifecycle without BURST's trend-direction m30 failure exit.

Known-data result: **rejected unchanged**. Historical fixed-size P&L was -₹153.04 at 500 ms (17 trades, 3 wins, PF 0.236) and -₹44.78 at 1 s (7 trades, 2 wins, PF 0.374). +$0.20 adverse slippage per side worsened both results. The frozen signal produced zero realized recent-24h opportunities on either grid.

No threshold loosening or post-result tuning is authorized.

Evidence: `docs/experiments/2026-09-25/34-v4-5-snapback-foundation.md` and `results/simulations/2026-09-25-v4_5-snapback-foundation/`.

## Experiment 35 — state-v2 walk-forward predictability

Experiment 35 is a **no-strategy-change, no-search** feasibility test for adaptive admission. The exact 20 state-v2 features plus engine one-hot were fed to a fixed ridge regression with training-only median imputation/z-scoring and `alpha=1.0`. No side feature, feature search, model-family search, threshold search or hyperparameter search was performed.

Historical expanding walk-forward did not show useful transfer: Spearman was -0.136 at 500 ms and -0.301 at 1 s. The later historical->recent transfer was essentially uncorrelated: +0.018 at 500 ms and -0.008 at 1 s. Predicted-positive recent subsets remained deeply negative.

The frozen Router-v2 score comparator was also nearly non-selective. Do not respond by tuning alpha/features/thresholds or escalating model flexibility on the same two windows.

Pre-run specification: `docs/plans/V4_5_ADAPTIVE_ADMISSION_FEASIBILITY.md`.

Evidence: `docs/experiments/2026-09-25/35-v4-5-state-v2-walkforward-predictability.md` and `results/simulations/2026-09-25-v4_5-state-v2-walkforward-predictability/`.



Evidence: `docs/experiments/2026-09-24/20-v4-5-exposure-velocity.md` and `results/simulations/2026-09-24-v4_5-exposure-velocity/`.

Further evidence: `docs/experiments/2026-09-24/21-v4-5-multi-ticket-horizon.md`, `results/simulations/2026-09-24-v4_5-multi-ticket-horizon/`, `docs/experiments/2026-09-24/22-recent-micro-long-validation.md`, and `results/simulations/2026-09-24-recent-micro-long-validation/`.

The 1 s grid remains a robustness diagnostic rather than the intended trading cadence, but a materially stronger 1 s result is desirable because it indicates less dependence on sub-second sampling.

## Experiment 21 — independent multi-ticket horizon branch

An alternative from-scratch research script tested up to three independent, same-direction XAUUSD tickets under one 3% aggregate planned-risk cap. The script chose longer trend or shorter impulse/reversal horizons from past-only canonical features; each ticket had its own executable-price profit confirmation, $4 emergency stop and time limit. This was **not** a change to Candidate D or the locked V4.4 implementation.

The initial full first-80% prototype lost ₹22.14 / ₹52.92 at 500 ms / 1 s. Two bounded seed revisions also failed joint-grid selection: +₹29.15 / -₹23.13 for an active impulse lane and +₹13.72 / -₹37.90 for a reversal lane. None attained ₹100 realized P&L on an observed UTC quote day. Cost stress worsened each 1 s result. No multi-ticket prototype is promoted or lock-eligible. Candidate D remains provisional. Experiment 22 added a fresh snapshot check and a rejected impulse-only ablation.

Evidence: `docs/experiments/2026-09-24/21-v4-5-multi-ticket-horizon.md` and `results/simulations/2026-09-24-v4_5-multi-ticket-horizon/`.

## Search discipline

- first 80% research pool only;
- 0-40% used for search/selection;
- later chronological segments used for evaluation;
- final 20% holdout remains sealed;
- 3% planned risk remains fixed;
- $4 emergency stop remains fixed unless a future documented research plan explicitly changes it;
- one shared position slot remains in force for Candidate D and the current composite; Experiment 21 separately tested a bounded three-ticket alternative without changing that candidate;
- no candidate is promoted from search score alone;
- execution-cost stress is required before any V4.5 lock.

## Lock direction

A strong V4.5 lock candidate should move toward:

- roughly 20% or better 500 ms capture if achieved without fragility;
- materially reduced 500 ms -> 1 s degradation, with ~10% 1 s capture treated as an aspirational robustness level rather than a forced threshold;
- higher profit velocity than provisional Candidate D;
- no material PF or drawdown deterioration;
- improvement or containment of the weak 50-60% regime;
- positive cost-stress behavior.

## Canonical implementation

Candidate B canonical implementation:

`research/canonical_replay.py --strategy v4_5_b`

Phase C1 research harness:

`research/v4_5_profit_velocity_search.py`

Phase C1 experiment record:

`docs/experiments/2026-09-24/17-v4-5-c-profit-velocity.md`

Phase C1 evidence:

`results/simulations/2026-09-24-v4_5-c-profit-velocity/`

Candidate D research implementation:

`research/v4_5_burst_path_autopsy.py` and `research/v4_5_confirmed_failure.py`

Completed exposure diagnostic:

`research/v4_5_exposure_velocity.py` (Experiment 20).

Completed negative independent multi-ticket research: `research/v4_5_multi_ticket_horizon.py` (Experiment 21). Its default reproduces the initial negative full-run prototype; its historical CLI keeps non-promoted revisions seed-only. Experiment 22's separate recent-data harness evaluated mode 3 on the later fresh segment. It is not a paper or live trading script.

Latest fresh-regime work: Experiment 26 replays the frozen v1 regime state; Experiment 27 freezes `xau-state-v2`; Experiments 28-32 show that router changes cannot manufacture missing edge; Experiment 33 confirms the existing four-engine family is independently negative on recent 500 ms and rarely produces simultaneous alternatives; Experiment 34 rejects frozen SNAPBACK unchanged; Experiment 35 rejects state-v2 ridge transfer as non-predictive; Experiment 36 retains outcome-blind `xau-microstate-v1` as a causal feature foundation only; Experiment 37 rejects the first frozen microstate-driven Candidate E after it sacrifices substantial 500 ms historical edge and fails to improve random-window mean expectancy. Candidate D remains the leader.

All canonical V4.5 research lives directly on `main`.


Current conclusion after Experiments 33-35: the next V4.5 research step must add genuinely new causal information or a structurally new opportunity hypothesis rather than retune the known windows. Candidate D remains leader and final20 remains sealed.

## Experiment 36 — raw-event microstate-v1 foundation

After rejecting fixed SNAPBACK and the no-search state-v2 ridge transfer test, Experiment 36 adds new causal raw-event information without changing strategy logic. The 23-feature microstate schema is frozen before outcome testing and excludes P&L labels from representation validation. Only coverage, cross-grid stability and distribution drift are allowed on known data. Any future signal/admission hypothesis using microstate-v1 must be frozen before outcome testing and still requires genuinely new non-overlapping validation for promotion.


### Experiment 36 result

`xau-microstate-v1` is retained as a causal representation foundation, not a candidate. All 23 features had 100% finite coverage on 551 known opportunity states. Strict <=10 s same-engine/same-side cross-grid pairs had median Spearman 0.869 historically and 0.935 on the recent window. No outcome labels were used. Because the known windows are now heavily observed, further strategy-changing selection must be prospective on a genuinely new non-overlapping raw snapshot; do not convert this representation result into another same-data threshold/model search.


## Experiment 37 — Candidate E PRIMARY microstate confirmation

Candidate E was frozen before outcome evaluation as Candidate D plus one narrow PRIMARY admission rule: defer the current sampled quote when both `dir_mid_move2 < 0` and `dir_event_imb2 < 0`. The rule used the natural zero boundary from frozen `xau-microstate-v1`, did not mutate cooldowns, did not fall through to another engine, and changed no exit, risk, spread or sizing rule.

Candidate D parity reproduced exactly. On canonical first80, Candidate E returned +₹1,163.47 / +₹374.58 at 500 ms / 1 s versus Candidate D's +₹1,430.31 / +₹385.25. On the already-known recent 24-hour window it improved losses from -₹223.27 / -₹135.91 to -₹216.77 / -₹94.08, including a large descriptive improvement on the recent 1 s evaluation split, but neither whole-window result became profitable. Forty deterministic real four-hour stress windows per grid had slightly worse mean expectancy than Candidate D on both grids.

Candidate E is therefore **rejected unchanged**. Do not tune this two-feature veto on the same known windows. Candidate D remains the provisional V4.5 leader. Experiment 36's `xau-microstate-v1` remains a retained causal representation foundation rather than a demonstrated trading edge. Final20 remains sealed.


## Experiments 38-43 — decision intelligence and path-preserving exits

Experiment 38 tested one fixed nonlinear decision policy using all frozen state-v2 + microstate-v1 entry features and five-second in-trade continuation snapshots. Entry EV did not rank trades reliably. The exit model reduced losses on the hostile recent snapshot but exited almost every trade and destroyed historical edge.

Experiment 39 changed the problem structurally: cooldown-free signal episodes for TAKE/WAIT/SKIP, 30-second continuation targets, causal trajectory deltas and two-negative confirmation. The learned policies still failed. A deterministic +1R activation / 50%-MFE profit ratchet was the only coherent mechanism.

Experiment 40 integrated that PRIMARY-only ratchet as Candidate F. It improved many random windows but early exits freed the shared slot, creating extra future trades and large path-dependent historical damage. Candidate F was rejected.

Experiment 41 introduced Candidate G ghost occupancy: after a real PRIMARY profit-protection exit, a capital-free virtual copy continues Candidate D's original lifecycle and keeps the slot/cooldown path unchanged. Entry-path parity passed exactly. This validated ghost occupancy as a reusable exit architecture, but the blunt ratchet still clipped too much historical upside.

Experiment 42 then gave entry selection the full signal "encyclopedia": all 20 state-v2 fields, all 23 microstate-v1 fields, cooldown-free signal episodes, and exact engine-specific rule-margin fingerprints. One fixed expert per engine predicted an exit-independent excursion target `(MFE - |MAE|) / $4`. Historical 500 ms episode selection improved descriptively, but the result did not transfer cleanly to 1 s or recent data. Recent PRIMARY was predicted positive on roughly 97% of checkpoints while its actual mean excursion edge was negative. The learned entry controller is not promoted.

Experiment 43 froze Candidate H, preserving Candidate D entries and Candidate G ghost occupancy while making PRIMARY profit retention signal-aware. The real exit requires +1R MFE, at least 1R giveback and two consecutive five-second checks where both direction-normalized raw two-second mid movement and raw event imbalance oppose the trade.

Candidate H results:

| Metric | 500 ms | 1 s |
| --- | ---: | ---: |
| Candidate D canonical first80 | +₹1,430.31 | +₹385.25 |
| Candidate H canonical first80 | +₹1,287.09 | +₹360.79 |
| D recent24h | -₹223.27 | -₹135.91 |
| H recent24h | **-₹208.47** | **-₹128.63** |
| D random 4h mean | -₹6.26 | -₹8.25 |
| H random 4h mean | **-₹3.36** | **-₹7.21** |
| D random positive fraction | 30.0% | 27.5% |
| H random positive fraction | **35.0%** | **30.0%** |

Candidate H also reduced 500 ms max segment drawdown from ₹218.94 to ₹148.66 and preserved exact entry-path parity. It is the strongest exit-management branch so far, but it is **not promoted** because it remains below Candidate D's canonical P&L and variable-window mean expectancy is still negative.

Current status: Candidate D remains provisional leader; Candidate H is retained as the preferred path-preserving exit branch for further structural work. Final20 remains sealed.


## Experiment 44 — tick-tape field audit

A representation-only audit checked whether the archived MT5 `last`, `volume`, `volume_real` and tick-flag fields could provide the missing trade-flow information for entry/exit intelligence.

They cannot on the current broker feed. Across 1,869,979 historical-first80 rows and 476,535 recent rows:

- nonzero `last`: 0%;
- nonzero `volume`: 0%;
- nonzero `volume_real`: 0%;
- LAST/VOLUME/BUY/SELL flags: 0 rows.

The BID and ASK change flags are populated on roughly 79% of rows and provide a useful exact quote-update mask. Flag-derived 2s/10s quote features were highly stable across matched 500 ms / 1 s opportunities: strict median Spearman 0.940 historical and 0.956 recent.

Therefore the current dataset supports richer **quote-event** microstructure but not true deal/aggressor flow. Future models must not treat inferred quote imbalance as actual traded buy/sell volume. A richer external/broker feed would be required to add that information class.

No strategy rule changed. Candidate D remains leader, Candidate H remains the strongest retained exit branch, and final20 remains sealed.


## Experiment 45 — external GC futures correlation bridge

A no-P&L alignment diagnostic fetched public 5-minute `GC=F` continuous Gold futures proxy bars for the already-known research windows and compared them with MT5 XAUUSD mid returns.

Raw timestamps looked unrelated because the MT5 archive is about three hours ahead of UTC market time. Both windows independently selected the same +180-minute GC shift relative to MT5-reported timestamps. At that alignment:

- historical first80: Pearson/Spearman returns 0.9921 / 0.9890, 95.05% direction agreement;
- recent24h: Pearson/Spearman returns 0.9915 / 0.9911, 95.83% direction agreement.

The independently recorded recent MT5-minus-host offset (-10,797.91 s applied as a correction) produced nearly the same result, confirming this is a clock-label issue rather than market lead.

Median GC-minus-MT5 basis was +$38.34/oz historically and +$34.83/oz recent24h. Use normalized returns/flow, not raw cross-venue price equality.

This validates COMEX Gold futures as a relevant external price-discovery sensor for the MT5 XAUUSD strategy stack. The 5-minute proxy does not provide actual trade tape/order book and cannot justify a strategy change by itself. The next genuinely new information class is richer CME trade/depth data aligned using the corrected timestamp basis.

Candidate D remains leader; Candidate H remains the strongest retained exit branch; final20 remains sealed.


## Experiment 46 — same-timeline external GC overlay simulation

Experiment 46 used the Experiment-45 external bridge inside the full path-dependent simulator. The external request period was derived only from archived MT5 timestamps; no current GC data was mixed into historical replay. The fixed -10,797.910989 second MT5 clock correction was used, and only the last fully completed five-minute GC bar was visible at each decision.

Frozen branches:
- GC-entry: skip PRIMARY/SECONDARY when direction-normalized GC 5m and 15m returns are both negative; preserve the skipped trade's slot/cooldown path with a capital-free legacy ghost.
- GC-exit: Candidate-H extra PRIMARY exit additionally requires both GC 5m and 15m returns to oppose the trade.
- Candidate I: both overlays combined.

Results:

| Metric | 500 ms | 1 s |
| --- | ---: | ---: |
| Candidate D canonical | +₹1,430.31 | +₹385.25 |
| Candidate H canonical | +₹1,287.09 | +₹360.79 |
| Candidate I canonical | +₹1,044.62 | +₹345.91 |
| D recent24h | -₹223.27 | -₹135.91 |
| H recent24h | -₹208.47 | -₹128.63 |
| I recent24h | -₹233.16 | -₹137.53 |
| D random 4h mean | -₹6.26 | -₹8.25 |
| H random 4h mean | -₹3.36 | -₹7.21 |
| I random 4h mean | -₹14.73 | -₹15.09 |

The external entry guard skipped 18 canonical trades at 500 ms and 22 at 1 s, sacrificing useful edge. The external exit confirmation blocked every Candidate-H extra exit at 500 ms and almost all at 1 s, showing that five-minute direction is too slow/coarse to validate the local decay that Candidate H observes.

Therefore the external-GC relationship is valid, but broad five-minute direction is not a useful individual-trade controller for this system. Candidate I is rejected unchanged. Future external integration should use higher-resolution GC trades/order-book depth or at minimum sub-five-minute futures features, aligned with the already-validated clock correction.

Candidate D remains leader; Candidate H remains the strongest retained exit branch; final20 remains sealed.

## Post-Experiment-46 architecture decision — MT5-only active runtime

External GC data is removed from the active V4.5 design. Experiments 45-46 remain useful evidence that COMEX Gold and broker XAUUSD are related, but the five-minute overlay failed as a trade controller and a second live data dependency is not justified.

The approximately three-hour discrepancy found during alignment was a timestamp-label/clock-offset issue rather than evidence that the external market is intrinsically three hours delayed. Nevertheless, deployability takes priority: the live algorithm must make entry and exit decisions from information available causally on the same MT5/broker feed used for execution.

Active-candidate feature gate:

- available from the same MT5 terminal/broker feed at decision time;
- reconstructable from archived MT5 data for replay;
- no mandatory external feed, cross-provider clock synchronization or secondary-network dependency;
- no feature retained merely because it correlates historically;
- any new feature must improve path-dependent simulation without sacrificing live practicality.

Therefore Candidate D remains the leader and Candidate H remains the preferred exit branch. External-GC code/evidence is archival research only, not runtime architecture. Final20 remains sealed.

# Experiment 31 — Router v2 arbitration divergence audit

Status: **execution pending**.

This bundle will explain the full raw-tick divergence between Candidate D and the frozen Experiment-28 Router v2 on already-known data.

The audit is diagnostic only. It must reproduce Experiment 30 exactly before its divergence records are accepted, and it does not modify any strategy rule, score, risk parameter or holdout boundary.

# Experiment 36 — microstate-v1 foundation

Status: **completed representation-only diagnostic**.

No P&L label was used to define or select the 23 raw-event features.

Results:

- 551 opportunity-state rows;
- 100% finite coverage for every feature;
- 163 strict historical and 33 strict recent cross-grid matches;
- median strict 500 ms / 1 s feature Spearman: **0.869 historical**, **0.935 recent**.

The representation is retained as a causal feature foundation, not as a strategy candidate.

Do not select thresholds or fit another known-data admission model from this result. The next strategy-changing test must be prospectively frozen and validated on genuinely new non-overlapping raw data.

# Experiment 32 — Router v3 priority-latched

Status: **execution pending**.

This is a pre-registered structural candidate, not a tuned score revision.

Router v3 preserves Candidate D engine priority, uses the frozen Router-v2 score only as a veto, and latches a rejected engine for its already-existing Candidate-D cooldown duration before allowing that same engine to be reconsidered.

Known seven-day/recent results are diagnostic only. A promotion decision requires genuinely new non-overlapping raw data.

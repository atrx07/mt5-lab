# Experiment 30 — full raw-tick Router v2 replay

Status: **execution pending**.

This is the first harness that runs the full Experiment-28 arbitration path rather than applying the score only to already-realized Candidate D entries.

It compares Candidate D and Router v2 on the canonical first80, the whole recent 24-hour raw snapshot, its historical split, and deterministic real four-hour random windows sampled from both known environments.

Known-data output is diagnostic only and must not be used to tune the frozen Router-v2 score.
